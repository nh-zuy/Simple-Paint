#ifndef TYPE_H
#define TYPE_H

/**
 * Classify type of existed tool
 *
 * @brief The ToolType enum
 */
enum class ToolType {
    Pen,
    Brush,
    Bucket,
    Eraser,
    Line,
    Rectangle,
    Circle
};

#endif // TYPE_H
